namespace GameFramework.ConfigGen
{
    // <auto-generated/> 枚举声明（wire 为 byte，值 = schema 中的顺序序号）
    // 顺序即 wire 序号：第 0 项是默认值（导出时该槽位不写，读回 0）。不要重排。

    public enum CoordinateSpace : byte
    {
        World = 0,   // 世界坐标系
        Local = 1,   // 相对父节点的局部坐标系
        Screen = 2,   // 屏幕或 UI 坐标系
    }

    public enum ItemRarity : byte
    {
        common = 0,   // 普通
        rare = 1,   // 稀有
        epic = 2,   // 史诗
    }

    public enum RewardKind : byte
    {
        Item = 0,   // 直接发放道具
        Currency = 1,   // 发放货币
        Bundle = 2,   // 发放一个奖励包
        Choice = 3,   // 从候选项中选择奖励
    }

    public enum ShowcaseRarity : byte
    {
        Common = 0,   // 普通品质，适合基础掉落
        Rare = 1,   // 稀有品质，出现概率较低
        Epic = 2,   // 史诗品质，通常拥有额外效果
        Legendary = 3,   // 传说品质，仅用于高价值奖励
    }

    public enum UIConfigLayer : byte
    {
        Page = 0,   // 页面
        Modal = 1,   // 模态
        Panel = 2,   // 面板
        Overlay = 3,   // 覆盖层
    }

}
