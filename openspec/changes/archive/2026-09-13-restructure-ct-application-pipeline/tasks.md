## 1. 固定基线与修复架构门禁

- [x] 1.1 记录基线提交 `95f04ad` 与实施时工作树状态；在临时数据工作区采集普通/过滤/强制导出的文件内容和元信息作为对照，交付基线记录，不改 gd 正式数据。→ `baseline/baseline.md` + `baseline/capture.py` + 4 份 JSON 采集（35 文件冷启动 / warm 零改写 / forced 字节等价 / 过滤 14 文件 / ref 拒绝）。采集时发现 gd 既有缺陷 A/B/C，A/B 不在本次范围（见 baseline.md §6）。
- [x] 1.2 修复 architecture 扫描器的 ct 前缀、单文件模块发现（含 `config.py` 这类单文件模块）、相对 import 与环节点归一化，移除不存在的 ct.validate 扫描目标；"禁止 ct 导入"规则须把**本包及其子模块**整体视为内部——现按 `imported != package` 字符串相等判断，会把 `diagnostics/__init__.py → ct.diagnostics.errors` 误报为违规。以非空扫描断言、违规下层依赖/相对环/合法图临时负例验证门禁确实生效。修好门禁会立即暴露 `export/deploy.py → ct.app.events`，须与 1.3 同一提交落地。→ 17 passed；门禁生效后额外抓出真实 import 环 `canonical_accessor ↔ canonical_accessor_model`（原第 54 行函数内导入），已把 `CSHARP_SCALAR_TYPES` 下移到 model 作为唯一来源修复。
- [x] 1.3 将事件与取消原语提炼到无 ct 依赖的 contracts，旧 app/events 兼容重导出，消除 deploy→app 反向依赖；运行真实源码依赖门禁并确认原事件调用方正常导入。→ 新增 `ct/contracts.py`（含 NullReporter）；`deploy.py` 改从 contracts 导入，`app/events.py` 兼容重导出，门禁 `_lower_layer_violations` 为空。

## 2. 共用校验与输入快照

- [x] 2.1 抽取布局/Excel/主键/CodeName/ref 共享 preparation 内核，保留 validate/export 的诊断包装和选中表范围；以现有校验测试及同输入两入口 issues 对照验证，无文件写入。→ 新增 `ct/app/data_preparation.py`（`prepare_tables` + `PreparedTable`/`PreparationResult` + 4 条校验规则，带 `before_table`/`after_table` 钩子保留 export 的逐表取消与日志交错）；`canonical_validate` 与 `run_canonical_export` 阶段 1 均改为调用内核，删除了重复实现。新增 5 个对照测试（两入口 issues 逐条一致 / validate 零写入 / 未知表 / 缺 Excel 两入口语义）；`tests/app tests/excel tests/architecture tests/cutover` 145 passed，且四模式产物与基线**逐字节一致**。
- [x] 2.2 定义 ExportRequest、CompletionPolicy、PreparedExport、TableBuild、ArtifactSet、ExportResult；原 canonical 函数保留参数/返回/异常契约，验证旧调用入口及返回字段兼容且没有第二套校验算法。→ 新增 `ct/app/exporting/models.py`（六个类型 + `InputChangedError`，`ExportResult.to_legacy_dict()` 保持旧返回形状）；`run_canonical_export` 内部构造 `ExportRequest` 并统一返回 `ExportResult(...).to_legacy_dict()`，signature/异常契约不变。`tests/app/test_exporting_models.py` 断言旧字段集合逐字一致、过滤与 forced 传递、两种完成策略、容器默认空。校验算法只剩 `data_preparation` 一套（由 2.1 的两入口一致性测试保证）。
- [x] 2.3 让配置、资源、Excel、译文和旧 manifest 从捕获内容解析，保留原文件路径诊断；验证 Excel hash 等于实际解析内容，并检查生成器不修改共享行数据。→ 新增 `ct/app/exporting/prepare.py`（`InputRevision`/`FileRevision` + `capture_export_inputs` + `verify_inputs_unchanged`）；Excel 已改为**从捕获字节解析**（`read_canonical_excel(..., data=...)` 走 `BytesIO`），账本 hash 取自已解析字节；生成器不改共享行、`_merge_i18n` 不就地修改入参，均有测试。**本轮补齐**：`load_config(..., text=)`、`YamlResourceRepository(..., contents=)`（资源集合也由捕获映射决定，不再 glob）、`CanonicalWorkspace.load(..., contents=, config=)`、`load_translation(..., data=)`、`load_manifest(..., data=)`，配套 `capture_sources` / `capture_translation_contents` / `capture_manifest_contents`；`run_pipeline` 全程只消费捕获字节。3 个决定性测试（`test_generation_consumes_captured_{translation,schema,manifest}`）证明磁盘与捕获内容不一致时产物反映**捕获**的那一份（译文文本、JSON 根键、manifest revision 41→42）。
- [x] 2.4 实现捕获前后与发布前的输入 revision/目录成员复核；测试 Excel/译文修改、schema 新增删除、global.yaml 路径变更均在发布前失败，最终复核后修改仍不被记为已导出。→ `verify_inputs_unchanged` 在捕获后与生成完成后各复核一次（`when` 区分「捕获期间」/「生成期间」）；`tests/app/test_input_snapshot.py` 9 passed 覆盖：Excel 修改、译文修改、schema 新增、schema 删除、global.yaml 路径变更全部中止，以及**最终复核之后**改磁盘仍保留捕获版本 hash（不冒充已导出输入）。layout manifest 作为发布目标不计入变更复核（见 `prepare.consumed_paths` 注释）。

## 3. 构建与正式写入分离

- [x] 3.1 按五阶段顺序从导出入口提取 build，JSON、bytes、Accessor、FBS、Bundle 返回 payload 或私有暂存引用；注入末段 FBS/Bundle 失败，验证正式 output 与 manifest 内容和 mtime 完全不变。→ 新增 `_Publication`（stage/commit：先收集 payload，全部生成成功后才 `atomic_write`）；删除了构建期的 `mkdir` 与源目录模板直写（模板改为 `build_canonical_template()` 产出字节并入发布集合）。`tests/app/test_publication_boundary.py` 验证注入 FBS 检查失败 / Bundle 失败后 output 与 manifest 的内容与 mtime 均不变，首次导出失败不创建半套产物。
- [x] 3.2 把 uniform/i18n layout 信息收敛到 TableBuild 并生成 manifest payload；运行定宽切换、稀疏 i18n 同序等长及 C#/Lua 生成器测试，验证字节与基线相同。→ 三个并行字典（`layout_info`/`table_bytes`/`i18n_table_bytes`）合并为 `builds: dict[str, TableBuild]`，`TableBuild` 新增 `i18n_slot_offsets` 与 `to_layout_info()`；manifest 改为 `manifest_payload()` 产出文本。定宽切换（`test_uniform_transition_invalidates_accessor`）、稀疏 i18n 同序等长、C#/Lua 生成器与访问器测试全部通过；四模式产物与基线逐字节一致。
- [x] 3.3 继续使用现有有效输入内容寻址缓存，保持版本失效与修复语义；运行增量测试覆盖 warm/forced 等价、mtime、译文/具名依赖/版本变化及损坏缓存恢复，不接入第二套复用决策。→ `ArtifactCache` 仍是唯一复用机制（`decide_artifact_reuse` 依旧无生产调用方，未接线）；`tests/app/test_incremental_export.py` 16 项全通过，覆盖 warm/forced 等价与 mtime、译文变化、具名依赖传递、生成器版本失效、损坏缓存与校验和不符恢复、缺失/被改写产物修复、热缓存仍阻止非法数据。
- [x] 3.4 显式计算 expected 与删除集合，维持局部 Bundle、共享产物和次语言过滤仍生成主语言 JSON；对照过滤基线验证范围不扩大，私有暂存不被陈旧清理枚举。→ `_Publication.stale_in(roots, exclude=...)` 显式计算 `deletions`；`STAGING_SUBDIR`（`output/.ct-staging`）在枚举前排除。测试确认：过滤导出不产出未选中表任何文件、不删除范围外产物、共享产物仍导出、`--lang en` 仍生成主语言 JSON、全量导出清理陈旧产物、私有暂存文件不被误删。另与 `baseline/filtered.json`（14 文件、无未选中表）一致。

## 4. 可恢复文件发布

- [x] 4.1 实现 `.ct/export-publication.json` 的版本化记录、同目标卷暂存及完整备份预检；以配置路径分离测试和备份中途失败测试确认尚未改写正式文件，变更 cache_dir 后仍能找到原恢复记录。→ 新增 `ct/storage/`（`files.py` 提供单一原子写实现，`cache/artifacts` 改为复用；`publication.py` 提供 `FilePublisher`）。journal 固定 `<root>/.ct/export-publication.json`（`JOURNAL_FORMAT = export-publication/1`，含 operation_id、规范化 root、允许目录白名单、逐目标 op/existed/old_hash/new_hash/staged/backup/done）。暂存用目标同目录 tempfile ⇒ 最终 `os.replace` 不跨卷（`test_staging_happens_on_the_target_volume` 断言 replace 源/目标同目录）。备份用 `copy2` 并校验 hash，**全部备份完成前不改写任何正式目标**（`test_backup_failure_never_touches_formal_files`）。`test_journal_path_does_not_depend_on_cache_dir` 证明改 cache_dir 后仍找得到原记录。
- [x] 4.2 实现 prepared/backed_up/publishing/committed 状态及逐文件替换/删除，纳入导出 manifest 和保留的模板新增分支；在覆盖、新建、删除的每个故障点验证恢复原内容、mtime 和文件存在性。→ 四阶段写 journal（临时文件 + replace）；`prepared` 只清理私有资源、`backed_up`/`publishing` 回滚旧集合、`committed` 只清理。manifest 通过 `emit(..., count=False)` 进入同一发布集合，模板缺失分支改为 `build_canonical_template()` 产出字节一并发布。`tests/storage/test_publication.py` 覆盖替换后崩溃（恢复内容+mtime）、新建文件（回滚删除）、删除中断（找回已删文件）；`test_manifest_participates_in_the_recoverable_transaction` 证明 manifest 与 output 同事务恢复。
- [x] 4.3 实现幂等恢复：未提交回滚、已提交清理；以子进程中断及重复恢复验证，特别覆盖 replace 后进度记录前崩溃、部分备份、全量删除中断。→ `recover()` 幂等（重复调用安全返回 None）；`test_subprocess_crash_leaves_recoverable_journal` 用**真实子进程 `os._exit(9)`** 在首个 replace 后无清理地死亡，下一次恢复把工作区还原到发布前（含 mtime）；`test_crash_after_first_replace_restores_everything`、`test_crash_during_deletion_restores_deleted_files`、`test_committed_journal_is_only_cleaned_up`、`test_recovery_is_idempotent`。
- [x] 4.4 对恢复记录损坏、格式未知、路径越界和恢复失败保留材料并拒绝继续；测试确认不静默跳过恢复、不自动部署或推进账本；validate/status 只读检测并报告未完成或损坏记录，且不写任何文件。→ `read_journal()` 对 JSON 损坏 / 格式未知 / 越界路径分别抛 `PublicationError` 且保留材料（3 项测试）；备份缺失时 `recover()` 报错而非静默跳过；`prepared` 阶段只清理、`committed` 阶段不回滚。新增 `canonical_publication_state()`（只读，不恢复不写盘），`canonical_validate` 将其作为工作区级问题报出，`ct status` 打印「未完成的发布」且不再输出 `[OK]`；`ct export`/`ct deploy` 在执行前先 `recover()`。
- [x] 4.5 接入发布期间延后取消与非强制相同内容不替换；测试提交前取消保持旧版本、提交中取消最终成功不误报 cancelled，warm manifest mtime 不变。→ 取消检查只在 prepare/build 阶段（逐表 + 逐语言），进入 publish 后不再检查 ⇒ 事务内取消被延后。`test_cancel_before_publication_keeps_old_version`（产物与账本均不变）、`test_cancel_during_publication_still_succeeds`（written 非空、事务正常提交、不误报 cancelled）；非强制同内容不替换由 `_Publication.stage` 的复用判定 + `test_warm_export_skips_generators_and_preserves_mtimes`（含 manifest mtime 断言）覆盖。

## 5. 统一完成服务与进程间协调

- [x] 5.1 实现规范化 root 的 `.ct/export.lock`、进程内互斥及双平台 advisory lock；用双进程测试验证同工作区冲突报 busy、进程死亡可重入、不同工作区独立执行。→ 新增 `ct/storage/workspace_lock.py`（`WorkspaceLock` + `WorkspaceBusyError`）：POSIX `flock(LOCK_EX|LOCK_NB)` / Windows `msvcrt.locking(LK_NBLCK)` + 按规范化 root 的进程内互斥；锁文件固定 `<root>/.ct/export.lock`，不随 cache_dir 变化；冲突立即抛 busy，不排队。`tests/storage/test_workspace_lock.py` 8 项：同进程二次获取 busy、上下文退出可重入、**文件存在 ≠ 已加锁**、不同 root 独立、**真实子进程**持锁时跨进程 busy、**kill 持锁进程后新请求可获取**（文件残留但不阻塞）。**接线（持锁恢复/prepare/build/publish/账本）属于 5.2/5.3，尚未进行**。
- [x] 5.2 在共享服务中串联持锁恢复、prepare/build/publish、入口部署策略和原子成功账本；测试 CLI 部署失败保留新本地产物与旧账本，账本失败不报成功，部分范围保留其他记录。→ 新增 `ct/app/exporting/service.py`：`workspace_transaction(root, reporter)`（持锁 → 先恢复未完成发布）、`run_export(request, policy, reporter, cancel_token, notify, on_deploy)`、`export_only(...)`。完成顺序 = 锁 → 恢复 → pipeline → 通知 → 按策略部署 → 原子账本。`tests/app/test_export_service.py` 9 项：export_only 记账、**不递归抢锁**（进程内锁不可重入，能跑通即证明只取一次）、busy 报错、部署失败保留新本地产物+旧账本、账本失败不报成功且已发布产物保留、部分范围保留其他表记录、notify 先于 deploy、Web 策略即使配置启用 deploy 也不部署、入口先恢复现场。
- [x] 5.3 CLI export/deploy 接入服务与呈现回调，保持本地导出完成→部署结果的输出顺序及 flags；测试热缓存仍部署、for-build、非法过滤、发布 I/O 失败和 busy 的友好非零退出。→ `ct export` 改调 `run_export(..., policy=export_then_deploy(for_build), notify=..., on_deploy=_echo_deploy_result)`，删除了适配器里的 `persist_export_state` 与 `_run_deploy`+记账编排（`ct deploy` 保留独立 `_run_deploy` 并同样先恢复）。`tests/app/test_cli_and_web_service.py` 覆盖：输出顺序 `导出完成` → `[deploy]`、`--for-build` 透传、busy 非零退出且提示友好、非法表/语言非零退出且不记账、部署失败非零退出且旧账本不变、独立 `ct deploy` 先恢复。
- [x] 5.4 Web 任务接入 export_only 服务并移除直接记账，保持路由、响应和阶段名称；测试配置启用 deploy 时 Web 仍不部署，失败/取消/成功历史与实际结果一致。→ `CanonicalExportTask._run` 改调 `run_export(..., policy=export_only(), reporter=PanelProgressReporter, cancel_token=...)`，删除了直接调用 `persist_export_state`；阶段名仍来自 `CANONICAL_STEPS`，`_finish_ok` 的 tables/elapsed 改读 `ExportResult` 字段。测试：配置启用 deploy 时 Web 不部署（deploy 桩零调用）但**仍记账**、`tables_exported` 正确、Excel 缺失时置 error 且不写账本。
- [x] 5.5 保留 run_canonical_export 只导出和原 dict 返回的兼容委托，保留 persist_export_state 的兼容入口；验证内部不递归抢锁，CLI/Web 不再分别编排成功记账，源码只有一个 pipeline。→ pipeline 唯一实现在新增的 `ct/app/exporting/build.py`；`ct/app/canonical_export.py` 变为纯兼容层（重导出 `CANONICAL_STEPS`/`CODEGEN_VERSION`/`STAGING_SUBDIR`/`persist_export_state` 等，`run_canonical_export` 委托 `service.export_only` 并 `to_legacy_dict()`）。架构门禁新增 `test_adapters_do_not_orchestrate_generators_or_the_ledger`（cli.py / web/tasks.py 不得出现 `run_pipeline(`/`persist_export_state`/`ArtifactCache`/生成器调用）；`test_compat_entry_exports_only` 断言兼容入口不部署、不记账且返回旧字段集合。**说明**：`build.py` 是 design 决策 1 模块表的落地（"build.py | 原五阶段顺序下的纯生成器调用、缓存及 layout 决策"）；把 pipeline 从 canonical_export 移出是打破 `canonical_export ↔ service` import 环的必要条件，由门禁 `test_no_dependency_cycle_imports` 捕获后修正。

## 6. 集成验收与文档

- [x] 6.1 在项目 venv 执行完整非浏览器回归与相关 Web 浏览器测试；验证新增发布契约、原 CLI/i18n/模板/Schema Apply 行为，保存实际命令和结果，环境缺失项不得记为通过。**通过**：`cd ct && .venv/bin/python -m pytest -q -p no:warnings -m "not browser"` → **551 passed, 0 failed, 21 deselected**；`pytest -m browser` → **21 passed**（Playwright Chromium 151.0.7922.34 实测可用）。新增发布/快照/服务契约（`tests/storage/` 24 + `tests/app/` 新增 42）全绿；原 CLI/i18n/模板/Schema Apply 行为全绿。中途曾出现 2 项失败，**全部来自实施期间工作树中一处非本作者的生成器改动**（`ct/src/ct/export/canonical_binary.py`，见 `baseline/baseline.md`「采集后的外部并发改动」）：① `test_uniform_assertion_fires_when_slot_is_omitted`；② `test_sparse_i18n_is_much_smaller_than_full_copy`。按本 change 的 Non-goals（不重写生成器）我未自行修改，**该外部作者随后自行更新了这两处断言并新增 `tests/export/test_uniform_layout.py`**，现已全绿。
- [x] 6.2 用基线工作区逐文件比较 JSON/FBS/Binary/Accessor，并运行既有 C# binary reader 集成；确认主语言/次语言、CodeName、具名类型、uniform、过滤/强制模式均兼容。→ 基线比对在**每个阶段**复跑：Phase 1–5 期间四模式（normal/filtered/filtered_ref_blocked/forced）与 `baseline/*.json` **逐文件 sha256 完全一致**。**外部并发改动之后**比对变为差异，且**差异恰好只落在 uniform 表**（Item/ItemType/Quest 的 manifest、Accessor 与 Bundle；非 uniform 的 UIConfig 不变），已归因到 `plan_object_layout`（见 `baseline/baseline.md`）——**基线未被重新采集**，以免把他人的改动当成自己的基线。C# 集成在新布局下**重新运行**：`prepare.py` + `gen_scalars_bench.py` + `dotnet run -c Release`（.NET 10.0.201）→ **共校验 127 个字段值，不一致 0**，覆盖 uniform 与变长表、CodeName 索引、FNV 对照、稀疏 i18n 三语言、12 种标量往返 ⇒ 新的 schema 推导偏移与 C# 运行期读法一致。跑完已 `git checkout -- test-proj/ExportAccessorVerify` 还原脚手架（`git status test-proj/` 为空）。
- [ ] 6.3 在 macOS/Windows 验证锁、文件占用故障、恢复和冻结 runtime 导出 smoke；记录平台结果与恢复后回退操作，任一必需平台未验证则保留未完成状态。**部分完成（macOS 已验、Windows 无法验证）**：macOS 上已验证跨进程锁冲突、kill 持锁进程后可重入、`os._exit(9)` 崩溃后幂等恢复、备份失败不改写正式文件（`tests/storage/test_workspace_lock.py` 8 项 + `test_publication.py` 16 项）。**冻结 runtime 导出 smoke 已完成**：`ct/.venv/bin/python -m PyInstaller --noconfirm --distpath dist --workpath build packaging/ct.spec`（PyInstaller 6.22.0）产出 `ct/dist/ct-runtime/ct`；用冻结二进制对基线工作区副本执行冷启动导出、warm 二次导出与 `validate` 全部成功，且 36 个产物与 venv 版 `ct` **逐字节一致**（`dist/`、`build/` 已被 `ct/.gitignore` 忽略）。**仍未完成、按要求保留未完成状态**：(a) Windows 侧 advisory lock 路径（`msvcrt.locking`）与文件占用故障无法在本环境执行；(b) ~~Flutter 桌面壳打包（本机无 Flutter SDK）~~ —— 但其 PyInstaller 冻结部分已单独验证。

> **归档后更正（2026-09-13）**：上一条 (b) 的前提是**错的**。当时那条探测只查了 `PATH`（`which flutter`），没有去尝试 `launcher/tool/build_macos.sh` 自己列出的候选路径，于是得出了「本机无 Flutter SDK」的错误结论。Flutter 实际位于 `/Users/tobeychao/development/flutter/bin/flutter`（stable 3.47.0），Xcode 26.6 与 CocoaPods 也齐备。
>
> 据此补跑完整打包：`FLUTTER=/Users/tobeychao/development/flutter/bin/flutter launcher/tool/build_macos.sh` → PyInstaller 冻结 + `flutter build macos --release` 产出 `ct_launcher.app`（93.8MB）+ 嵌入 runtime + ad-hoc 签名，**四步全过**。包内 runtime 验证：`codesign --verify --deep --strict` 通过；`ct_launcher.app/Contents/Resources/runtime/ct export --root <副本>` 导出 4 张表成功、`validate` 通过，且 **36 个产物与 venv 版 `ct` 逐字节一致**；`launcher/build/` 已被 `launcher/.gitignore` 忽略。
>
> 因此 6.3 的 **macOS 侧已全部验证完毕**（含 Flutter 桌面壳打包）。仍未验证的只剩 **Windows**：`msvcrt.locking` 路径与文件占用故障，需在 Windows 机器上跑 `tests/storage/test_workspace_lock.py` 与 `launcher/tool/build_windows.ps1`。复选框保持未勾选不变。
- [x] 6.4 更新工具架构/缓存/恢复文档，说明 `.ct/` 私有文件、生成缓存与账本区别、外部编辑与跨工作区共享目标限制；文档中的模块路径和失败行为与实现测试逐项核对。→ `ct/docs/README.md`：工作区结构新增 `.ct/`（锁与恢复记录、建议 gitignore、文件存在 ≠ 已加锁）；新增「缓存、账本与发布的关系」表、「一次导出的写入边界」、「明确不提供的保证」（外部编辑不受锁保护、跨工作区共享目标、validate/status 只读、Schema Apply 是独立协议）；`ct status` 输出示例补 `[publication]` 行。`AGENTS.md`：目录结构补 `.ct/` 与 `cache/artifacts` + `state.json`、**修正 `output/json` 为 `{Table}_{lang}.json`（原写「按语言分目录」，与实现不符）**、模块表补齐 `contracts`/`data_preparation`/`exporting/*`/`storage/*` 并把 `canonical_export` 标为兼容层、新增「可恢复发布与工作区锁」决策条目。文档中的模块路径与失败行为均对照实现测试核对。
- [x] 6.5 实施验收后同步本 change 的四份 delta，并更新 incremental-export Purpose 和 unity-deploy 过时的范围说明；同步时须重写 incremental-export 中为"归档连续性"保留的旧 Scenario 标题（`--all 不是行为开关`、`未变化的表仍被重建`、`指纹变化不改变导出行为`、`未被引用的复用判定`），使标题与新契约一致——Scenario 标题在归档后会原样进入主规范，不能只靠 delta 里的说明段纠正。不把历史未实现的部署可见性算作本次完成，运行 OpenSpec 严格校验并确认本 change 所有任务都有验证证据。→ 四份 delta 已按 `openspec-sync-specs` 语义合并进主规范（无 artifact rules）：**新建** `openspec/specs/export-publication/spec.md`（Purpose 逐字来自 delta + 7 条 ADDED requirement）；**cli-interface** 的 `ct export command` 按 delta 重写并补 `Forced export`、`Deploy failure keeps the old ledger` 两个 scenario；**unity-deploy** 的 `无变化时仍可部署` 去掉了「未实现/前提不存在」警示块并改为目标契约（2 scenario），顶部实现范围说明更新为 2026-09-13 复核口径（「部署状态可见」仍标注**未实现**，未算作本次成果）；**incremental-export** 应用 2 处 RENAMED + 3 处 MODIFIED，**重写了 4 个旧 Scenario 标题**（`未变化的表仍被重建`→`未变化的表仍被解析校验`、`--all 不是行为开关`→`--all 强制重建并写出`、`指纹变化不改变导出行为`→`有效译文变化只更新相关产物`、`未被引用的复用判定`→`复用不依赖未接线的指纹辅助函数`），并重写 Purpose（原文写「当前每次都是全量重建，没有任何产物复用路径」，与实现矛盾）；未在 delta 中提及的 `ct status 只报 missing / changed / drifted` 与「事务化 Apply」两条按原样保留（前者补一句说明 `[publication]` 行归 `export-publication` 能力管，避免与新条目冲突）。`openspec validate --specs` → **21 passed, 0 failed**；`openspec validate restructure-ct-application-pipeline --strict` → valid；主规范内无残留 delta 头。
