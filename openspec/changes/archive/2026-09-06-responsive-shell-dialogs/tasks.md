# Tasks

## 1. 样式与资产

- [x] 1.1 在 SVG defs 新增 `<symbol id="i-search">`（放大镜），并验证任意 `<use href="#i-search">` 可渲染
- [x] 1.2 新增 `.btn-danger`（实心红：`var(--danger)` 底白字 + hover 加深）与 `.btn-danger:disabled` 灰态，验证按钮视觉与禁用态
- [x] 1.3 新增 `draftbar` 样式（accent 圆点 + 文本 + 撤销/重做 ghost-sm + 审查并应用 primary，`flex:0 0 auto`、warn 警告态），验证在 Schema 页 flex 底部正确排布
- [x] 1.4 为弹窗宽度变体加样式，沿用现有 `.dlg-wide` 前缀：`.dialog.dlg-sm`（小号 420）、`.dialog.dlg-plan`（变更计划 640）、`.dialog.dlg-palette`（调色板 480），验证不同弹窗宽度生效
- [x] 1.5 低宽度/矮视口适配：`.dialog` 改 flex 列 + `max-height:calc(100svh - 32px)`、`.dbody` `flex:1 1 auto;min-height:0` 内部滚动、`.mask` 加 `overflow-y:auto` + `.dialog` 加 `margin:auto`、`.dfoot` 与 `draftbar` 允许 wrap，验证横屏矮窗与窄屏不裁切、按钮不溢出

## 2. 草稿状态条

- [x] 2.1 在 Schema 页 `#wb` 之后插入 `.draftbar`（初始隐藏），验证无命令时不占位
- [x] 2.2 实现 mock 草稿状态对象（命令数/撤销栈）+ 撤销/重做 disabled 逻辑，验证按钮随状态切换
- [x] 2.3 接线「审查并应用」→ 打开 P5 变更计划弹窗，验证点击弹出
- [x] 2.4 持久化失败警告：mock 触发 IndexedDB 写入失败（如 mock 开关置 `persistFail=true`），draftbar 变 warn 底色 + 「草稿未持久化」文案，内存草稿仍可编辑、警告持续显示不消失，验证（对应 spec 场景）

## 3. 删除资源（D1）

- [x] 3.1 mhead「删除资源」打开 D1 弹窗：资源标识（kind 徽标 + mono 名）+ 影响面 + 安全网文案 + [查看影响]/[取消]/[删除]，验证打开与初始焦点在取消
- [x] 3.2 反向引用 blocker：默认活动资源 Item 被引用（ItemType.Id、DropRange）→ 弹窗显示红字 blocker、删除按钮 disabled；切到未引用资源（如 ItemType）→ 无 blocker、删除可用，验证两种状态（切换机制：mock 资源引用表，按当前选中资源取引用集）
- [x] 3.3 点「删除」→ 命令入草稿、状态条出现「1 条未应用变更」、弹窗关闭还焦，验证

## 4. 删除字段（D2）

- [x] 4.1 字段行删除按钮打开 D2 小号弹窗（Name · string + 安全网 + [取消]/[删除]），验证打开与焦点
- [x] 4.2 点「删除」→ 命令入草稿 + 弹窗关闭，验证状态条计数增加

## 5. 添加字段（F1 + F2）

- [x] 5.1 「添加字段」打开 F1：字段名（必填+命名校验行内提示）/ 类型触发器 / 角色单选互斥（无|I18N|Server-only）+ 主键/必填复选 / 约束分隔符（仅 vector 显示），验证表单渲染与互斥
- [x] 5.2 命名校验：非法名红框 + 行内提示、不提交；合法名 Enter/「添加字段」入草稿并关闭，验证
- [x] 5.3 F2 类型选择器：搜索 + 分组列表（标量/具名带 kind 标签/容器）+ 点选回填 F1 类型行 + vector 时显示分隔符，验证

## 6. 变更计划（P5）

- [x] 6.1 打开 P5：风险徽标 + 影响清单（Schema/Excel/FBS/Binary/Accessor · 表 · 动作）+ 阻塞项（**列出位置与样例值**，有则应用禁用）+ 放弃草稿/取消/应用变更 + 有效期 hint「计划有效期 2 小时」，验证
- [x] 6.2 应用成功 → 弹窗关闭、草稿清空、状态条消失、draftbar 槽位短暂显示成功态「已应用：Item · Quest」后自动隐藏，验证
- [x] 6.3 「放弃草稿」→ 打开 P6 小号确认（放弃 N 条未应用变更？[取消][放弃]，初始焦点取消）→ 确认后清空命令、状态条消失，验证

## 7. 选择表（P1）

- [x] 7.1 toolbar「选择表」打开 P1：搜索 + 4 状态 pill + 计数 + 行（表名/字段数/i18n 数/状态徽标），初始焦点搜索框，验证
- [x] 7.2 点选行关闭并回填 toolbar 值 + **翻译表随之切换（表格数据替换为该表）** + 无匹配空态，验证

## 8. 关于 / 帮助 / 导出文档

- [x] 8.1 侧栏「关于」打开 I1（logo+版本/工作区+简介+外链），验证
- [x] 8.2 侧栏「帮助与反馈」打开 I2（快捷键速查+反馈外链），验证
- [x] 8.3 「导出文档」标记外部链接（`title="外部文档"`），点击不弹窗，验证

## 9. 快速打开（Q1）

- [x] 9.1 rhead 新增 ⌕ ibtn（`#quick-open`）+ 全局 Cmd/Ctrl+P，验证两种触发方式
- [x] 9.2 调色板：输入即滤 / ↑↓ / Enter 打开（rhead 标题 + 资源树高亮）/ Esc 先清词再关 / 空态 / 关闭还焦 ⌕，验证

## 10. 非弹窗接线

- [x] 10.1 字段上移/下移：点击交换相邻行；首行上移/末行下移禁用（clamp，同真实面板 schema.js），验证
- [x] 10.2 i18n 行「保存」：若该行存在未提交的行内 textarea 则先提交（commitInlineEdit），随后按钮短暂显示「已保存」再恢复，验证（对齐现有全屏/行内编辑器）
- [x] 10.3 导出页「重新导出」：触发 mock 进度推进（prog-cell 逐格 active/done），验证
- [x] 10.4 pills 过滤（语言/状态/模块/级别/范围）与搜索（资源/日志）过滤生效，验证
- [x] 10.5 日志/历史「前往导出」跳转到导出页，验证

## 11. 验证

- [x] 11.1 Playwright 全量点击冒烟：逐个触发所有弹窗/状态条/接线项，断言无 JS 报错、弹窗开合正确；**断言每个弹窗初始焦点符合 spec（danger→取消 / 表单→首输入 / picker→搜索框）；断言嵌套弹窗（P5→P6）下 Escape 与遮罩点击先关最上层（P6）再关 P5**，验证
- [x] 11.2 断点抽查（900/740 上下）截图比对：桌面弹窗居中、窄屏弹窗不越界、侧栏抽屉不与弹窗冲突，验证
- [x] 11.3 矮视口抽查（横屏 568×320 与窄屏 320×568）截图：弹窗不裁切、body 可滚动、dfoot/draftbar 按钮不溢出，验证
