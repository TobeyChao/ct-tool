## 1. 弹窗契约骨架

- [x] 1.1 新增 `js/core/dialog.js` 弹窗栈：`openDialog({variant,title,body,actions,initialFocus,onClose})`，实现 Esc 关最上层、焦点陷阱过滤 disabled、标题 id 计数器（dlg-title-N）、关闭还焦触发元素、背景点击关闭、对下层与 main 应用 inert；`components.css` 落 `.ct-dlg-sm/.ct-dlg-plan/.ct-dlg-palette/.ct-dlg-wide` 宽度变体。验证：浏览器冒烟（打开/嵌套两层/Esc 逐层关/Tab 不逃逸/还焦）
- [x] 1.2 补 `.btn-danger` 实心 danger 样式与 disabled 视觉态、`.btn-danger-ghost` 描边变体（不得只靠颜色区分，配文案）。验证：浏览器检查禁用态对比度与焦点样式

## 2. Schema 新弹窗（prompt 清零）

- [x] 2.1 D2 删除字段确认弹窗（sm 变体）：动态字段名与类型，初始焦点「取消」，确认后命令入草稿。验证：浏览器测试——取消不入草稿、确认后 draftbar 计数 +1
- [x] 2.2 D1 删除资源确认弹窗（模块头「删除资源」danger-ghost 入口）：显示资源种类/字段数/行数与「先入草稿可撤销」说明；反向引用 blocker 列表时禁用删除并注明不提供级联删除；提供「查看影响」按钮；初始焦点「取消」。验证：浏览器测试——被引用/未被引用两分支
- [x] 2.3 改名字段与枚举新增值弹窗替换 `prompt()`（表单变体，行内校验非法名不关闭不提交）。验证：浏览器测试改名/加值成功路径与非法输入路径
- [x] 2.4 现有类型选择器迁移到 dialog.js 并提取为可嵌套复用函数（搜索 + 标量/具名分组 + ref 行）。验证：点选回填、Esc 退栈回添加字段弹窗
- [x] 2.5 F1 添加字段弹窗：名称行内校验（大写开头、不以 `_` 结尾）、角色单选互斥（无/I18N/仅服务端）、I18N 锁 string（选择器仅列 string）、vector 修饰符（标量/Enum 变长内置分隔符、Record 强制定长 + excel_columns 默认 3、ref 禁勾、server 角色亦禁勾）、Code 代号勾选后锁名/类型/角色且一表至多一个、强制约束只读提示不进命令、主键 Id 不出现；提交前调 `/api/schema-workspace/validate` 兜底。验证：浏览器测试覆盖规格全部场景（含互斥与 Code 冲突）
- [x] 2.6 P5 变更计划弹窗（plan 变体）：风险徽标 + 按产物影响清单 + 阻塞项（位置/样例值）+ 计划有效期提示（2 小时），blocked 禁用应用；预览模式（删除弹窗「查看影响」进入：横幅标明尚未入草稿、应用/放弃禁用、关闭回删除弹窗）；成功后关闭并经草稿条显示「已应用」清单；「放弃草稿」sm 确认并连关计划弹窗。验证：浏览器测试——阻塞分支/成功分支/放弃确认/预览模式

## 3. 存量弹窗迁移

- [x] 3.1 快速打开迁移为 palette 变体，⌘P / 键盘导航 / 最近打开行为不变。验证：现有 fuzzy/quick-open 浏览器测试全绿
- [x] 3.2 i18n 选择表 / 翻译进度 / 清理确认三个弹窗迁移到 dialog.js（背景点击、Esc、还焦行为统一）。验证：`pytest tests/web/test_module_pages_browser.py` 全绿

## 4. 壳层与投影重构

- [x] 4.1 `app-shell.js` 重写：常驻侧栏（品牌 + 模块文字导航 + 底部「导出文档/帮助/关于」）+ main 底部 draftbar 宿主；再次点击当前 Schema 条目切换资源 pane；移除桌面顶栏、图标活动栏、底部导航；`<740` 顶栏（汉堡 + 品牌标记 + 工作区根目录名）+ 侧栏汉堡抽屉。验证：浏览器测试侧栏切换模块、抽屉开关、无底部导航
- [x] 4.2 `core/projection.js` 改两断点状态机（≥900 停靠 / 740–899 pane 抽屉 / <740 全抽屉）；pane 停靠态默认折叠，编辑器头部「资源面板」按钮 + 属性活动 Tab 开合（IDE 式激活态）；resize 只收不展（≥900→<900 收双 pane、≥1200→900-1199 收属性 pane、<740→≥740 关侧栏抽屉）；schema 模块 `window.innerWidth` 阈值判断全部改为消费投影状态；移除页面栈（pushState/popstate/history.back 调用）。验证：`pytest tests/web/test_schema_editor_browser.py` 全绿
- [x] 4.3 `layout.css` 断点重构（900/740）：pane 抽屉化（transform + backdrop + inert，740-899 抽屉贴顶）、Esc 退栈序 dialog > 侧栏抽屉 > 面板抽屉 > 列菜单；字段表 ≥900 保持表格（容器内横滚 + 右缘滚动光晕 + 操作列固定宽），<900 转字段卡片（表头隐藏、名称+操作/类型/Excel/角色分区、操作常显）；移除手机底部导航 CSS。验证：`test_matrix_browser.py`（断点参数改 900/740）全绿，390×844 无页面级横向滚动
- [x] 4.4 关于 / 帮助与反馈弹窗 + 导出文档外部链接（侧栏底部三入口，帮助快捷键文案与实际一致）。验证：浏览器测试三入口行为

## 5. 草稿条与快捷键

- [x] 5.1 schema 模块派发 `ct:draft`（pending/canUndo/canRedo/warn），shell 渲染 draftbar：待应用光标计数、归零后「已全部撤销 · 可重做」态、持久化失败警告态、应用成功「已应用：…」success 态数秒后隐藏；draftbar 按钮 `ct:draft-action` 反向接线。验证：浏览器测试——推 2 撤 2 显示可重做态、警告态常驻、成功态自动消失
- [x] 5.2 ⌘Z / ⇧⌘Z 全局草稿撤销/重做快捷键（与 ⌘P 并存）。验证：浏览器测试键盘撤销/重做与 draftbar 同步

## 6. i18n 增强

- [x] 6.1 译文单元格全屏角标 + 全屏编辑弹窗（wide 变体、标题含主键·字段·当前语言、原文只读对照、进入前先提交行内编辑、保存走 `saveEntry` 写回并联动状态徽标与过滤、取消/Esc/背景不写回）；行内编辑行为保留。验证：`test_module_pages_browser.py` 新增全屏编辑用例（保存/取消/行内保留）
- [x] 6.2 原文列两行截断「展开/收起」尾标（页面激活、行渲染、resize 后重评截断）+ 工具栏「列」显隐菜单（原文/译文/状态/操作四列，勾选即时生效，切表与行更新后随状态过滤重放）。验证：浏览器测试展开收起、隐藏列、切表重放

## 7. 收尾与验收

- [x] 7.1 清理死代码：四档投影残留、底部导航 CSS、页面栈残留、旧弹窗实现。验证：grep 无残留引用，`pytest tests/web/` 全绿
- [x] 7.2 视口矩阵验收：1600×900 / 1360×768 / 1280×720 / 960×640 / 720×460 / 390×844 与 100%/125%/150% 缩放，覆盖断点穿越（1400→800→1400 只收不展、1200 属性档）与默认折叠首屏、无错位遮挡、无页面级横向滚动。验证：`test_matrix_browser.py` 全绿 + 截图人工复核
