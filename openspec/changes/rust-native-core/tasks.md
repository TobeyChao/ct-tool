## 1. 基线、协议与技术验证

- [ ] 1.1 固定当前可验收源码树/依赖及未提交相关变更的指纹，以 coverage.md 固定业务兼容矩阵，验证覆盖现行 CLI、Schema、Excel、i18n 与发布测试。
- [ ] 1.2 建立 S/M/L 临时夹具和四种基准场景，运行 Python 基线，保存硬件、种子、原始耗时/RSS/产物摘要，确认未写真实 gd。
- [ ] 1.3 建立 native Cargo workspace 和 CI 基础，验证三平台编译及独立 CLI smoke。
- [ ] 1.4 输出协议 v1 文档/机器 schema/同源样例，覆盖方法、握手、事件、取消、分页、错误与大整数；用契约测试验证样例并供 Flutter 消费。
- [ ] 1.5 验证 Excel 读取候选对公式缓存、日期、空值、错误值及固定 vector 的行为，与 openpyxl 夹具逐项对照。
- [ ] 1.6 验证 Excel 写入适配的表头样式与 stable column path 迁移，确认不可迁移数据拒绝覆盖并记录依赖选型结果。
- [ ] 1.7 跑通复杂表类型化解析和 uniform/非 uniform 二进制原型，对照字节和初步耗时后记录继续迁移的技术结论。

## 2. 领域模型与数据准备

- [ ] 2.1 实现配置路径、YAML 资源模型及类型表达式，验证自定义目录、非法字段和跨平台大小写冲突诊断。
- [ ] 2.2 实现资源依赖图、拓扑序、反向引用和循环检测，验证 Record/Enum/ref 的现行允许范围。
- [ ] 2.3 实现布局计划、manifest 读取兼容与行坐标映射，验证 Schema 漂移和缺 manifest 行为。
- [ ] 2.4 实现 scalar/Enum/Record/vector 解析与精确数值处理，逐项通过边界和嵌套类型对照夹具。
- [ ] 2.5 实现主键、CodeName、枚举及 ref 闭包校验，验证单表过滤仍拒绝坏引用且问题顺序确定。
- [ ] 2.6 接通只读 validate/status，用目录快照验证不写缓存、不恢复事务且正确报告 pending journal。

## 3. 生成与完整业务用例

- [ ] 3.1 实现 JSON 输出及格式细节，验证所有语言/过滤组合与基线逐字节一致。
- [ ] 3.2 实现 FBS 文本及结构检查，验证传递类型和非法布局对照且正式导出不调用 flatc。
- [ ] 3.3 实现表级 FlatBuffers、定宽槽位及 vtable 检查，验证默认值/空表/嵌套向量及布局对照。
- [ ] 3.4 实现查询索引与 Bundle 组装，验证哈希碰撞、顺序及局部 Bundle 字节一致。
- [ ] 3.5 实现 C# Accessor 和枚举生成，通过文本对照及独立 C# 读取测试。
- [ ] 3.6 实现 Lua Accessor 和枚举生成，通过文本对照及独立 Lua 读取夹具。
- [ ] 3.7 实现 i18n 合并和稀疏语言产物，验证主语言 fallback、过滤与三语言输出。
- [ ] 3.8 实现 i18n sync/status/compact 与条目保存，验证四态、confirmed、dry-run、过滤和无关文件保持。
- [ ] 3.9 实现模板新建及保留数据迁移，验证表头样式、列路径迁移、无 manifest 拒绝和失败原文件不变。
- [ ] 3.10 实现 Schema 命令重放、候选及净差异，验证创建/改名/删除、undo/redo、引用和跨类别名称冲突。
- [ ] 3.11 实现 Schema revision/candidateHash 与 YAML-only 保存服务，验证旧基线拒绝、目录成员变化和事务失败保留原文件。

- [ ] 3.12 对照 canonical hash/resourceId/revision/candidateHash 和默认值省略规则，验证升级不误报 drifted、无变化保存不重排 YAML，以及 Excel 路径归属冲突拒绝。
- [ ] 3.13 增加非首个活跃 Sheet、公式缓存缺失、日期、空行坐标、富文本和超长 Enum 下拉夹具，验证读取/模板语义且无任意工作簿无损保留的错误承诺。

## 4. 可靠存储与入口策略

- [ ] 4.1 实现规范化工作区锁，运行新旧进程互斥、进程死亡释放和 Windows 锁区间对照测试。
- [ ] 4.2 实现兼容 journal 的多文件发布/恢复，在各阶段注入失败，验证旧文件/mtime 恢复和新增文件清理。
- [ ] 4.3 实现输入捕获与发布前复核，验证 Excel/YAML/译文和目录成员在构建期间变化会阻止发布。
- [ ] 4.4 实现部署及独立部署用例，验证目标范围、错误和不更新成功账本的行为。
- [ ] 4.5 实现 CLI/桌面完成策略、晚取消和 state.json，验证部署或记账失败不误报成功，部分导出保留其他账本条目。

- [ ] 4.6 实现旧 apply-journal/1 适配、workspace.recover 和只读快照恢复阻塞，验证完整回滚/已提交清理/备份缺失保留、残留apply.lock不忙碌和恢复后旧基线拒绝。

## 5. 增量和并行优化

- [ ] 5.1 实现类型化解析缓存及版本/校验和校验，验证同 mtime 内容变更、损坏、缺失和版本升级均正确重建。
- [ ] 5.2 实现局部及 ref 校验依赖指纹，验证目标主键删除、Record/Enum 变更触发所需失效且不漏报非法数据。
- [ ] 5.3 实现共享分层产物指纹及原始 bytes 缓存，验证翻译无效元信息变化不重建、输出缺失/改写可修复。
- [ ] 5.4 实现按工作簿/表的有界并行与稳定汇总，验证不同并发度输出和诊断顺序一致并记录峰值内存。
- [ ] 5.5 优化诊断探测和大产物暂存，保留现有诊断语义，使用阶段 profile 证明减少重复构建/复制。
- [ ] 5.6 实现 --all 绕过所有计算缓存及强制写出，对每个增量场景比较成功失败、字节和 mtime 契约。

## 6. CLI、worker 与发布验收

- [ ] 6.1 实现兼容 CLI 参数、文本与 JSON 输出，覆盖过滤错误、退出码和 panel 迁移指引的命令级测试。
- [ ] 6.2 实现 worker 握手、请求路由及所有业务方法，使用独立协议客户端完成完整操作链，不依赖 Flutter。
- [ ] 6.3 实现控制消息响应、有界事件队列、分页及安全 shutdown，验证忙碌时取消、超限/坏消息、断连和大整数不丢精度。
- [ ] 6.4 运行所有兼容/故障注入/并发对照，交付产物摘要与诊断差异报告，未解决差异不得切换生产入口。
- [ ] 6.5 在 Windows/macOS/Linux 按配对测量规范运行四种性能场景，提交原始结果与门槛比较，不达标继续优化或显式修订提案。
- [ ] 6.6 生成各目标平台独立运行时包和版本信息，在无 Python 环境验证 CLI/worker 及中文空格路径，提供给桌面打包。
- [ ] 6.7 演练旧 journal 恢复和旧版本回滚，更新 CLI 安装/迁移/缓存说明，验证新生产入口无 Python 回退。
- [ ] 6.8 在实际验收完成后同步相关规格及 launcher/incremental-export 的旧 Purpose 描述；保留实施证据，未通过项不得勾选或用归档代替验收。

- [ ] 6.9 更新原生安装/构建/测试和旧Python隔离对照入口，验证 PATH 不混淆、gd 无工具依赖，对齐 tool-workspace-separation delta。
- [ ] 6.10 实现成功桌面导出历史兼容读写、模块日志、任务问题分页与dismiss，验证配置cache_dir、旧中文状态、重启保留和历史失败不误报业务提交失败。
- [ ] 6.11 验证分页revision/重复requestId/候选代次/协议错误分类，用同源Dart/Rust契约夹具覆盖乱序、重复、重连和输入变化。
- [ ] 6.12 逐行完成 coverage.md 的内核验收映射，显式检查 server_only、游戏读取缓存/语言切换、部署.meta/for-build/缺源保护；报告未实现历史规格的排除理由。
