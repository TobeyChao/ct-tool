// <auto-generated/>
// Canonical C# accessor for Quest ()
using System;
using System.Collections.Generic;

public static partial class QuestAccessor
{
    private const string TableName = "Quest";
    /// <summary>行数。</summary>
    public static int Count => Runtime.Count(TableName);

    /// <summary>按主键查行；未找到返回 null。</summary>
    public static QuestRow? ByID(int id)
    {
        IntPtr p = Runtime.ByID(TableName, id);
        return p == IntPtr.Zero ? (QuestRow?)null : new QuestRow(p, Runtime.Version(TableName));
    }

    /// <summary>按 Excel 序行下标取行；越界返回 null。</summary>
    public static QuestRow? ByIndex(int i)
    {
        IntPtr p = Runtime.RowAt(TableName, i);
        return p == IntPtr.Zero ? (QuestRow?)null : new QuestRow(p, Runtime.Version(TableName));
    }
}

public unsafe readonly struct QuestRow
{
    private readonly IntPtr _row;
    private readonly int _version;
    internal QuestRow(IntPtr row, int version) { _row = row; _version = version; }
    public int Id => WireReader.I32(_row, 4);
    public string Title => new NString((byte*)WireReader.Indirect(_row, 6), _version);
    public string Description => new NString((byte*)WireReader.Indirect(_row, 8), _version);
    public int RewardItemId => WireReader.I32(_row, 10);
    public ItemRow? Item => ItemAccessor.ByID(RewardItemId);
    public int RequiredLevel => WireReader.I32(_row, 12);
}