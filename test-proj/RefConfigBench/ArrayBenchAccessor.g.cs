// <auto-generated/>
// Canonical C# accessor for ArrayBench
using System;
using System.Collections.Generic;
using System.Runtime.CompilerServices;

public static partial class ArrayBenchAccessor
{
    private const string TableName = "ArrayBench";
    private const int MaxSlot = 20;
    private static ConfigTable _table;
    private static int _tableVersion = -1;

    /// <summary>解析并缓存表句柄；整套 bin 换代后 Runtime 会重建表对象。</summary>
    [MethodImpl(MethodImplOptions.NoInlining)]
    private static void Resolve()
    {
        _table = Runtime.Table(TableName);
        _tableVersion = TableVersion.Current;
    }

    internal static ConfigTable Table
    {
        get
        {
            ConfigTable t = _table;
            // 世代守卫：整套 bin 换代（LoadBundle/Clear）后必须重新取句柄，
            // 否则会继续用已被 Dispose 的旧表缓冲（悬垂指针）。
            if (t != null && _tableVersion == TableVersion.Current)
            {
                return t;
            }
            Resolve();
            return _table;
        }
    }

    /// <summary>行数。</summary>
    public static int Count => Table.Count;

    /// <summary>按主键查行；未找到返回 null。</summary>
    public static ArrayBenchRow? ByID(int id)
    {
        ConfigTable t = Table;
        int idx;
        IntPtr p = t.ByID(id, out idx);
        if (p == IntPtr.Zero)
        {
            return null;
        }
        else
        {
            return new ArrayBenchRow(p, t.OffsetsFor(p, MaxSlot), t.Version, idx);
        }
    }

    /// <summary>按 Excel 序行下标取行；越界返回 null。</summary>
    public static ArrayBenchRow? ByIndex(int i)
    {
        ConfigTable t = Table;
        IntPtr p = t.RowAt(i);
        if (p == IntPtr.Zero)
        {
            return null;
        }
        else
        {
            return new ArrayBenchRow(p, t.OffsetsFor(p, MaxSlot), t.Version, i);
        }
    }
}

public unsafe readonly struct ArrayBenchRow
{
    private readonly IntPtr _row;
    private readonly int[] _off;
    private readonly int _version;
    private readonly int _index;
    internal ArrayBenchRow(IntPtr row, int[] offsets, int version, int rowIndex)
    {
        _row = row;
        _off = offsets;
        _version = version;
        _index = rowIndex;
    }
    public int Id => WireReader.I32At(_row, _off[4]);
    public int Type => WireReader.I32At(_row, _off[6]);
    public int Group => WireReader.I32At(_row, _off[8]);
    public NArray<int> Areas
    {
        get
        {
            return new NArray<int>((byte*)WireReader.IndirectAt(_row, _off[10]), _version);
        }
    }
    public NArray<int> TargetAreas
    {
        get
        {
            return new NArray<int>((byte*)WireReader.IndirectAt(_row, _off[12]), _version);
        }
    }
    public NArray<int> PopAreas
    {
        get
        {
            return new NArray<int>((byte*)WireReader.IndirectAt(_row, _off[14]), _version);
        }
    }
    public NArray<int> Forbidden
    {
        get
        {
            return new NArray<int>((byte*)WireReader.IndirectAt(_row, _off[16]), _version);
        }
    }
    public NArray<int> Connections
    {
        get
        {
            return new NArray<int>((byte*)WireReader.IndirectAt(_row, _off[18]), _version);
        }
    }
}