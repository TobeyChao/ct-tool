// <auto-generated/>
// Canonical C# accessor for ItemBench
using System;
using System.Collections.Generic;
using System.Runtime.CompilerServices;

public static partial class ItemBenchAccessor
{
    private const string TableName = "ItemBench";
    private const int MaxSlot = 34;
    private static ConfigTable _table;
    private static int _tableVersion = -1;

    /// <summary>解析并缓存表句柄；整套 bin 换代后 Runtime 会重建表对象。</summary>
    [MethodImpl(MethodImplOptions.NoInlining)]
    private static void Resolve()
    {
        _table = Runtime.Table(TableName);
        _tableVersion = TableVersion.Current;
    }

    internal static ConfigTable Table
    {
        get
        {
            ConfigTable t = _table;
            // 世代守卫：整套 bin 换代（LoadBundle/Clear）后必须重新取句柄，
            // 否则会继续用已被 Dispose 的旧表缓冲（悬垂指针）。
            if (t != null && _tableVersion == TableVersion.Current)
            {
                return t;
            }
            Resolve();
            return _table;
        }
    }

    /// <summary>行数。</summary>
    public static int Count => Table.Count;

    /// <summary>按主键查行；未找到返回 null。</summary>
    public static ItemBenchRow? ByID(int id)
    {
        ConfigTable t = Table;
        int idx;
        IntPtr p = t.ByID(id, out idx);
        if (p == IntPtr.Zero)
        {
            return null;
        }
        else
        {
            return new ItemBenchRow(p, t.OffsetsFor(p, MaxSlot), t.Version, idx);
        }
    }

    /// <summary>按 Excel 序行下标取行；越界返回 null。</summary>
    public static ItemBenchRow? ByIndex(int i)
    {
        ConfigTable t = Table;
        IntPtr p = t.RowAt(i);
        if (p == IntPtr.Zero)
        {
            return null;
        }
        else
        {
            return new ItemBenchRow(p, t.OffsetsFor(p, MaxSlot), t.Version, i);
        }
    }

    // ---- per-field 字符串缓存（行下标索引，整套 bin 换代时整体重建）----
    private static string[] _designNameCache;
    private static string[] _descriptionCache;
    private static string[] _bpDescriptionCache;
    private static int _strCacheVersion = -1;

    [MethodImpl(MethodImplOptions.NoInlining)]
    private static void ResetStringCaches()
    {
        int n = Table.Count;
        _designNameCache = new string[n];
        _descriptionCache = new string[n];
        _bpDescriptionCache = new string[n];
        _strCacheVersion = TableVersion.Current;
    }

    internal static string[] DesignNameCache
    {
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        get
        {
            string[] c = _designNameCache;
            if (c != null && _strCacheVersion == TableVersion.Current)
            {
                return c;
            }
            ResetStringCaches();
            return _designNameCache;
        }
    }

    internal static string[] DescriptionCache
    {
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        get
        {
            string[] c = _descriptionCache;
            if (c != null && _strCacheVersion == TableVersion.Current)
            {
                return c;
            }
            ResetStringCaches();
            return _descriptionCache;
        }
    }

    internal static string[] BpDescriptionCache
    {
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        get
        {
            string[] c = _bpDescriptionCache;
            if (c != null && _strCacheVersion == TableVersion.Current)
            {
                return c;
            }
            ResetStringCaches();
            return _bpDescriptionCache;
        }
    }

    public unsafe readonly struct DropRangeRow
    {
        private readonly IntPtr _row;
        private readonly int _version;
        internal DropRangeRow(IntPtr row, int version)
        {
            _row = row;
            _version = version;
        }
        public int Min => WireReader.I32(_row, 4);
        public int Max => WireReader.I32(_row, 6);
    }

}

public unsafe readonly struct ItemBenchRow
{
    private readonly IntPtr _row;
    private readonly int[] _off;
    private readonly int _version;
    private readonly int _index;
    internal ItemBenchRow(IntPtr row, int[] offsets, int version, int rowIndex)
    {
        _row = row;
        _off = offsets;
        _version = version;
        _index = rowIndex;
    }
    public int Id => WireReader.I32At(_row, _off[4]);
    public string DesignName
    {
        get
        {
            string[] c = ItemBenchAccessor.DesignNameCache;
            string s = c[_index];
            if (s != null)
            {
                return s;
            }
            s = NStringCache.Decode((byte*)WireReader.IndirectAt(_row, _off[6]));
            c[_index] = s;
            return s;
        }
    }
    public int Type => WireReader.I32At(_row, _off[8]);
    public int Quality => WireReader.I32At(_row, _off[10]);
    public int IconRes => WireReader.I32At(_row, _off[12]);
    public int PileCount => WireReader.I32At(_row, _off[14]);
    public int SellPrice => WireReader.I32At(_row, _off[16]);
    public float PriceChecker => WireReader.F32At(_row, _off[18]);
    public int IsPut => WireReader.I32At(_row, _off[20]);
    public bool Hidden => WireReader.BoolAt(_row, _off[22]);
    public string Description
    {
        get
        {
            string[] c = ItemBenchAccessor.DescriptionCache;
            string s = c[_index];
            if (s != null)
            {
                return s;
            }
            s = NStringCache.Decode((byte*)WireReader.IndirectAt(_row, _off[24]));
            c[_index] = s;
            return s;
        }
    }
    public string BpDescription
    {
        get
        {
            string[] c = ItemBenchAccessor.BpDescriptionCache;
            string s = c[_index];
            if (s != null)
            {
                return s;
            }
            s = NStringCache.Decode((byte*)WireReader.IndirectAt(_row, _off[26]));
            c[_index] = s;
            return s;
        }
    }
    public NStructArray<NString> StringParams
    {
        get
        {
            return new NStructArray<NString>((byte*)WireReader.IndirectAt(_row, _off[28]), _version);
        }
    }
    public NArray<int> Tags
    {
        get
        {
            return new NArray<int>((byte*)WireReader.IndirectAt(_row, _off[30]), _version);
        }
    }
    public ItemBenchAccessor.DropRangeRow Drop
    {
        get
        {
            return new ItemBenchAccessor.DropRangeRow((IntPtr)WireReader.IndirectAt(_row, _off[32]), _version);
        }
    }
}